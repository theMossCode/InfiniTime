import { PaginationState, PaginationOrder } from "./types";
import { Dispatch, SetStateAction } from "react";
export declare const paginationStateFactory: (setState: Dispatch<SetStateAction<PaginationState>>) => {
    setPage: (page: number) => void;
    setRowsPerPage: (rowsPerPage: number) => void;
    sort: ({ order, orderBy }: {
        order: PaginationOrder;
        orderBy: string;
    }) => void;
    nextPage: () => void;
    previousPage: () => void;
};
export declare const usePaginationState: (initialState: PaginationState) => {
    setPage: (page: number) => void;
    setRowsPerPage: (rowsPerPage: number) => void;
    sort: ({ order, orderBy }: {
        order: PaginationOrder;
        orderBy: string;
    }) => void;
    nextPage: () => void;
    previousPage: () => void;
    page: number;
    rowsPerPage: number;
    order: "asc" | "desc" | undefined;
    orderBy: string | undefined;
};
